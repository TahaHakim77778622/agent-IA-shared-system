version: '3.8'

services:
  # Base de données MySQL
  mysql:
    image: mysql:8.0
    container_name: taha_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: taha_db
      MYSQL_USER: taha_user
      MYSQL_PASSWORD: taha_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - taha_network

  # Backend FastAPI
  backend:
    build: .
    container_name: fastapi_backend
    restart: always
    environment:
      - DATABASE_URL=mysql+pymysql://taha_user:taha_password@mysql:3306/taha_db
      - SECRET_KEY=your-super-secret-key-here-change-in-production
      - HOST=0.0.0.0
      - PORT=8000
    ports:
      - "8000:8000"
    depends_on:
      - mysql
    networks:
      - taha_network
    volumes:
      - ./:/app
    command: python run.py

volumes:
  mysql_data:

networks:
  taha_network:
    driver: bridge 